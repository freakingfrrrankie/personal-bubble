{
  "name": "presence-server",
  "version": "1.0.0",
  "description": "Minimal WebSocket server that broadcasts the live number of connected visitors",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
